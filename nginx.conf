events {
    worker_connections 180;
}

http {
    access_log off;
    upstream localhost {
        least_conn;
        server api01:8080;
        server api02:8080;
    }

    server {
        listen 9999;
        location / {
            proxy_pass http://localhost;
        }
    }
}